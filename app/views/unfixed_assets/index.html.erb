<% @page_title = "Manage Unfixed Assets" %>

<%= render 'shared/title', title: link_to("Asset Management", 
                                          asset_management_path,
                                          class: "ks-red-light") + 
                                          " &raquo; ".html_safe + 
                                          "Unfixed Assets" %>

<div class="row">
  <div class="col-xs-12 col-md-12">
    
    <div class="row text-center">
      <%= render 'paginate', small: true %>
    </div>
    
    <table class="table table-hover">
      <%= render 'ufa_table_header' %>
      <%= render 'ufa_table' %>
    </table>
    
    <div class="row text-center">
      <%= render 'paginate' %>
    </div>
    
  </div>
</div>